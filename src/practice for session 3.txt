一、写一个基类（Person）和派生类(Employee)，满足下列要求：
1. Person类中定义两个构造函数，分别以name和name，age作为入参；
2. Employee采用继承构造，同时完成dept(部门)成员变量的初始化
3. 编写调用函数对两个类进行基本的验证并总结继承构造在代码复用
方面的作用；
4. 将基类Person改为抽象类，练习override和final的作用

二、Lambda练习

1、基于上述的Employee类，增加成员函数（这个自己可以定义）并在其
内部定义lambda操作，体会lambda作为函数指针对编程带来的便捷，包括
变量捕获。
2.建立一个int类型的vector容器， 例如{1，3，4，3，8，9}，统计容器中
3的数量（用lambda表达式）；


